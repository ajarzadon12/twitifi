.card
    - @followees.each do |followees|
        %div.twits.clearfix
            .col-md-1.no-padding
                - if followees.user.image_url
                    = image_tag followees.user.image_url, class: 'avatar'
                - else
                    = image_tag 'https://s3.amazonaws.com/mypresonus/profile/1432960478_922038.jpg', class: 'avatar'
            .col-md-11
                %b= "#{followees.user.first_name} #{followees.user.last_name}"
                %span.text-muted= followees.user.email
                .btn-group.pull-right.text-muted
                    %button.btn.btn-default.dropdown-toggle{"aria-expanded" => "false", "aria-haspopup" => "true", "data-toggle" => "dropdown", :type => "button"}
                        %span.glyphicon.glyphicon-chevron-down
                    %ul.dropdown-menu
                        %li
                            = link_to 'Delete', post, :method => :delete, :data => { :confirm => 'Are you sure?' }
                %p= post.text
            %hr
